{
  "name": "pulse-survey-bot",
  "version": "1.0.0",
  "description": "Anonymous pulse survey Slack app for ERGs",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@slack/web-api": "^7.8.0",
    "@netlify/blobs": "^8.1.0"
  },
  "devDependencies": {
    "@netlify/functions": "^3.0.0"
  }
}
